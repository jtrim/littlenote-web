// Generated by CoffeeScript 1.3.3
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  App.LoginCommand = (function(_super) {

    __extends(LoginCommand, _super);

    function LoginCommand() {
      return LoginCommand.__super__.constructor.apply(this, arguments);
    }

    LoginCommand.prototype.mode = 'email';

    LoginCommand.prototype.initialize = function() {
      App.StatusBar.show("Enter your email");
      return LoginCommand.__super__.initialize.apply(this, arguments);
    };

    LoginCommand.prototype["return"] = function() {
      switch (this.mode) {
        case "email":
          App.StatusBar.show("Enter your password");
          this.email = this.$el.val();
          this.mode = "password";
          this.$el.val('');
          return this.$el.attr('type', 'password');
        case "password":
          this.password = this.$el.val();
          this.$el.val('');
          this.$el.attr('type', 'text');
          return this.submitRegistration();
      }
    };

    LoginCommand.prototype.submitRegistration = function() {
      var _this = this;
      return $.ajax({
        url: "/login",
        contentType: 'application/json',
        type: "POST",
        data: JSON.stringify({
          email: this.email,
          password: this.password
        })
      }).done(function() {
        return App.StatusBar.show("Signed in as " + _this.email + ".");
      }).fail(function(jqxhr) {
        if (jqxhr.status === 422) {
          return App.StatusBar.show("Error: " + jqxhr.responseText, true);
        } else {
          return App.StatusBar.show("Sorry, something went wrong.", true);
        }
      }).always(function() {
        return _this.tearDown();
      });
    };

    return LoginCommand;

  })(App.Command);

}).call(this);
